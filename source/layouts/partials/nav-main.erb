<% classes ||= "main" %>

<nav class="<%= classes %>" role="navigation">
    <ul>
        <% data.chapters.each do |chapter| %>
        <li class="nav-item"><a href="/<%= chapter.slug.urlize %>/"><%= chapter.slug %></a></li>
        <% end %>
    </ul>
</nav>
